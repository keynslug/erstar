%% -*- erlang -*-

{deps, [
]}.

{lib_dirs, ["deps"]}.
{erl_opts, [
    debug_info,
    bin_opt_info,
    warn_missing_spec
]}.

{eunit_opts, [verbose]}.

{cover_enabled, true}.
{cover_print_enabled, true}.
